name: to_do_firebase
description: "A new Flutter project."

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none'  # Remove this line if you wish to publish to pub.dev

# Version and build number for your application.
# Change version: X.Y.Z+build_number
version: 1.0.0+1

environment:
  sdk: ">=3.5.4 <4.0.0"  # Ensure compatibility with the specified Dart SDK version.

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  gap: ^3.0.1
  flutter_riverpod: ^2.0.0
  intl: ^0.20.1
  firebase_core: ^3.9.0
  cloud_firestore: ^5.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Lints to encourage good coding practices.
  flutter_lints: ^5.0.0

flutter:
  # Ensure Material Icons font is included.
  uses-material-design: true

  # Add assets to your application.
  assets:
    - assets/profile.png
